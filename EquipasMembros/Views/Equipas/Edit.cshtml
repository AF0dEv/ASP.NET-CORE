@model EquipasMembros.Models.Equipa

@{
    ViewData["Title"] = "Edit";
}

<h1 class="h1 text-black">Edit</h1>

<h4 class="h4 text-black-50">Equipa</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="NomeEquipa" class="control-label text-black">Nome Equipa:</label>
                <input asp-for="NomeEquipa" class="form-control" />
                <span asp-validation-for="NomeEquipa" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="link-light" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
